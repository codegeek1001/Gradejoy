<h3>Add <%= @assignment.name %> to Another Course</h3>

<%= form_for @course_assignment do |f| %>
  <% if @course_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment.errors.count, "error") %> prohibited this assignment from being created:</h2>

      <ul>
      <% @course_assignment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
      <%= f.submit "Submit", class:"btn btn-primary" %>
  </div>

<% end %>
