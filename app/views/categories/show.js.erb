var categoryId = "#categoryid<%= j @category.id.to_s %>"
var categoryButtonsId = "#categorybuttons<%= @category.id.to_s %>"

$(categoryButtonsId).html("<%= j (render 'category_buttons') %>")
$(categoryId).click(function(){
  $(categoryButtonsId).toggle();
});
